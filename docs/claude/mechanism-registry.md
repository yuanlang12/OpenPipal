# 机制台账（Mechanism Registry）—— 分层与日落条件

> 判定公式：**"如果模型是完美的，这个机制还需要存在吗？"**
> 是 → 永久架构；否 → 能力拐杖，出生时必须登记日落条件。
> 配套原则见 CLAUDE.md「机制分层与日落」；设计哲学见 [design-philosophy.md](design-philosophy.md)。

## 第一层：永久架构（模型无关，越强越值钱）

竞态、并发、成本、信任边界、数据治理与模型智能无关。这一层放心加，永不日落。

| 机制 | 位置 | 解决的确定性问题 |
|---|---|---|
| artifact 注册表 + 同步落盘 | `pi-tools.ts` / `artifact-store.ts` | create→render/edit 的落盘竞态 |
| 按会话运行槽（`runs: Map<cid, {abort, agent}>`，取代旧的单一全局 `currentAbort`） | `ipc-handlers.ts` | 会话 B 发消息误杀会话 A 的活/后台流——2026-07 生成稳定性调查认定的"真·中断源"（用户体感的"经常中断"里唯一真实的一条）。已落地，steer/queue 现可作用于任意后台会话 |
| 前缀缓存三原则（stablePrefix / runtime-context 独立末尾消息并落盘回放 / 历史字节单调化）+ 按服务商设置缓存路由、保留时长和显式断点 | `pi-agent-service.ts` / `config-manager.ts`（`withSessionStreamOptions` + `resolveCacheRetentionForModel`）/ `model-prompt-cache-guide.md` / `pi-message-conversion.ts`（`buildRuntimeContextMessage` 实发与回放同源）**+ 三处历史投影必须携带 `messageKind`**：`chatStore.toApiMessages`（桌面）、`scheduler.storedMessageToChatMessage`（定时任务）、`http-server.ts`（ACP/插件） | token 成本。OpenAI、Anthropic、Bedrock 与兼容网关的断点字段不同；新增模型必须按维护指南核对官方协议、捕获请求体并用真实 cached token 验收，不能把专有字段广播给所有网关。**runtime-context 快照的落盘回放横跨主进程→preload→渲染层 store→再投影回主进程**：任何一处历史投影丢掉 `messageKind`，快照就退化成普通用户消息、回放字节与实发不符，跨回合缓存静默全失（2026-08-18 实案：三处投影同时丢标记，机制上线即失效，功能无异常、只体现在账单）。**读写两侧都要齐**：读侧是三处投影携带 `messageKind`，写侧是"谁把快照存进磁盘"——桌面靠 renderer，ACP / scheduler 没有渲染层，靠 `createTranscriptCollector.finishRuntimeContext()` 自己接住后紧跟 user 消息落盘（同日第二次发作：读侧修完写侧还空着，两条路照旧不命中）。新增"无渲染层"的入口必须同时补写侧|
| 历史整体压缩（按模型 Token 窗口保近概括远，触发=字符估算 ∨ 实报锚点双信号） | `history-compactor.ts` / `context-window-policy.ts` | 上下文经济学——平时保持追加式历史；单个工具结果首次进入时确定性限长；只有总量接近模型窗口时整体概括较早历史，不按年龄、工具名或“是否消费”卸载。**证据式预算**（2026-08-06）：服务商每轮实报的 prompt tokens（`recordMeasuredPromptTokens` 锚点）与字符估算取或触发——估算只覆盖历史消息且对代码/中文系统性低估，曾三层近似同向漂移致 129k 载荷静默穿过 99k 阈值（deepseek 128k 窗实案）；实测值覆盖完整载荷（system+tools+注入），对齐 pi-ai `estimateContextTokens` 的锚点思路。锚点按 model 校验不跨模型沿用，进程内存态、重启冷启动一轮回落估算 |
| 上下文溢出自愈（length+近零输出分型 → 强制压缩重建 → 原地重试一次） | `empty-completion-guard.ts`（`isContextOverflowCompletion`）/ `pi-agent-service.ts`（`rebuildAfterOverflow`） | 资源耗尽型失败的处方与流式抖动相反——窗口满时盲目续跑每次再追加消息只会更糟（2026-08-06 实案：三连撞墙全走了续跑）。判据对齐 pi-ai `utils/overflow` 判据三（pi 自家只在 pi-coding-agent AgentSession 消费，裸 Agent 层缺位，宿主自建）。压不动或重试仍空 → 报"窗口上限"真话文案（不再怪罪服务商）。窗口溢出是物理约束，模型再完美也会遇到，永久 |
| Agent Plugins 校验与失败边界（manifest 闭集校验 / 路径不出插件根 / env 禁嵌密钥与保留键 / 单 server 失败不连坐） | `plugin-manager.ts` | 第三方包进入本机执行链路的安全与数据治理——插件目录来自外部（GitHub/文件夹），路径逃逸、伪造 command、非环回 http 都是不可逆/高代价错误，硬拒绝正当。失败边界遵循规范：包无效→拒包；mcp.json 无效→只禁该包 MCP；单条 server 无效→只跳该条 |
| 工具轨迹跨轮完整回放 | `tool-trail.ts` / `pi-agent-service.ts`（convertHistoryToPiMessages）/ `chat/messages.ts`（放行 role:'tool'） | 上下文正确性——跨轮保留工具名、完整参数、结果、图片或附件引用并恢复成成对的 Pi toolCall/toolResult。单个超长结果由 `context-window-policy.ts` 保留头尾，总量由统一历史压缩兜底；不再存在类内预算、回执替换、三档衰减或消费后卸载 |
| 用量落盘（`usage.jsonl` 逐次调用记账：input/cacheRead/cacheWrite/output + `trailTok`/`trailMsgs` 轨迹占比 + 每轮汇总；8MB 滚一代；`usage-report.mjs` 按模型分组出命中率与轨迹占比） | `usage-log.ts` / `pi-agent-service.ts`（Usage 观测点）/ `scripts/diagnostics/usage-report.mjs` | 成本可观测性——多轮对话成本是 N²（一"轮"里每次工具调用都是一次完整重发），唯一的减法是前缀缓存，而**命中率不可推断、只能实测**（网关是否做隐式缓存、TTL 多长、前缀被哪次改写打断，都不写在任何文档里）。没有这份记录，缓存策略是否有效只能靠猜。只观测不改行为，写失败一律吞掉 |
| 瞬时断流重连（`isolateAbortSignalForStream` 内置**三道闸**：最多 5 次、从首次失败起算 5 分钟窗口、**同一失败原因连撞 2 次即放弃**；退避 1/2/4/4/4 秒；判据直接用 Pi 的 `isRetryableAssistantError`） | `isolated-stream-signal.ts` / `pi-core-runtime.ts` + `pi-agent-service.ts`（`onStreamRetry` → `stream_retry` 事件 + `adapter.reset()`） / `chatStore.ts`（丢弃半截内容 + 提示，成功后 `dropStreamRetryNotices` 撤掉提示） / `pi-event-adapter.ts`（`createTranscriptCollector.feed` 的 `stream_retry` 分支：无渲染层那两条路的写侧） | 传输可靠性——网关在思考流中途关掉 SSE 是物理事实，模型再完美也挡不住，套判定公式即永久。**证据**（2026-08-22 opencode-go/glm-5.3 实案）：五次失败落在 50/131/209/380/526 秒，没有固定阈值 → 不是超时是掉线；`outcome:"provider_error"` 而非 `"watchdog"`，错误原文是 pi 的 `Stream ended without finish_reason`（`openai-completions.js:440`：SSE 体读完却没有 finish_reason，即对面干净地关了响应，既不是我们掐的也不是 socket 崩的）。Pi 自己把 `"ended without"` 列进 `RETRYABLE_PROVIDER_ERROR_PATTERN` 并用 `retryAssistantCall` 退避重试三次，但那条路径只活在 pi-coding-agent 的 AgentSession 里；我们是裸 `Agent` + 自带 streamFn，**重试一次都没有**，断一次整轮报废。**重连 = 整轮重发，不是断点续传**（HTTP 流断了服务端不留半截思考），所以三件事必须成套：① 吞掉重连那次的 `start` 事件，否则 pi-agent-core 按 start 再 push 一条 assistant 消息、一次断流变两个气泡；② 渲染层收到 `stream_retry` 必须丢掉已上屏的思考/正文，否则两次尝试的内容被拼成一条（正文那半是数据损坏，不只是难看）；③ 退避睡眠挂在父 signal 上，用户按停止立刻醒，不用等满退避。**已知不覆盖**：失败发生在工具参数已经开始流出之后不重连——工具卡已经画在界面上，重来会画出第二张（工具本身在 assistant 消息完成前不执行，所以只有展示问题、没有副作用问题）。时间窗从**首次失败**起算而非本轮开始：慢失败（一次 400 秒）只够重连一次，快失败（一次 50 秒）能用满 5 次，自动匹配故障形态；最坏等待 ≈ 5 分钟 + 一次尝试的时长，全程停止键可用。**同因连撞闸**（2026-08-22 加）：只数次数不看"是不是一直在撞同一堵墙"，会把"1 分钟失败"拖成"7 分半失败"——同端点两轮实测 `[StreamProbe]` 数据，7 次尝试断 6 次、5 次尝试断 4 次，**重连 10 次 0 次救回**，每次都白烧一份完整输入。判代价：拿 4 次重连换延迟，偶发抖动仍留 1 次免费重来。**比较键必须归一化**（`failureKey`：抹数字 + 截 120 字符）——Pi 把网关原始响应体最多 4000 字符塞进 `errorMessage`（`utils/error-body.ts` `formatProviderError`），错误体里带 request id / trace id / 时间戳时原文全等比较每次都"不一样"，闸门会静默退化回 5 次。**写侧第二次踩同一个坑**：`stream_retry` 的"丢掉半截正文"规则最初只写在 `chatStore`，`createTranscriptCollector`（定时任务 / ACP，无渲染层）没有对应分支，两次尝试的正文会被粘成一条落进持久历史——与前缀缓存那条同款教训，**新增"无渲染层"入口必须同时补写侧**。**提示是实时状态行，不进记录**（2026-08-23 收口）：原来"成功后撤掉"只跑在前台 `onStreamEnd` 分支——用户切走、这一轮在后台收尾，那条"正在重试"就永远留在历史里（现在时永久停摆）。与其在后台分支再抄一遍撤销逻辑，不如把规则挪到记录边界：`conversation-store.ts` 的 `isLiveOnlyNotice` 在 `normalizeStoredMessages` 里过滤掉 `inject-notice/stream-retry`，读写共用同一口子（渲染层防抖保存、插件 HTTP、定时任务、ACP 都走这对 append/replace），前后台不对称因此不存在，旧记录里已经躺着的那些读取时一并消失。前台失败时提示仍留在屏幕上（那几分钟得有个交代），但只活到本次会话结束。**副作用**：删提示必须 `markDirty()` 走整份重写——落盘水位线数的是内存数组下标，中间删一条会让后面所有消息前移一格，尾部追加就漏一条真消息 |
| 三层安全 + artifact sidecar 写封锁 | `pi-security.ts` | 信任边界（写封锁兼有"产物存储单一写入方"的数据完整性不变量） |
| EPHEMERAL_ARTIFACT_TYPES 标准 | `artifact-store.ts` | 数据治理——垃圾输入带来垃圾输出 |
| 测试/对话统一管线 + content-type 守卫 + 辅助路径思考口径统一（`resolveAuxThinkingLevel`/`auxCompletionTuning`：标题生成/记忆抽取/dreamer/extractor/simple-completion/GoalChecker 与主链路同款"能力位→'low'"决策，开思考同步 +2048 maxTokens 余量） | `config-manager.ts` | 双路径漂移；200+HTML 假成功；硬编码关思考被强制思考模型 400 拒（2026-07-23 实案：qwen3.8-max-preview 致 TitleGen/记忆抽取/GoalChecker 每轮静默失败）。已知例外：subagent-runner 仍硬编码 'off'——子代理模型由 profile 钉住且刻意选低价档，若某 profile 钉强制思考模型会复现同款 400 |
| 能力位体系（supportsImages / supportsThinking / contextWindow / supportsEffortDial / thinkingAlwaysOn / thinkingLevels 派生） | `config-manager.ts` | 声明式模型适配——同时是拐杖的自动日落开关。Qwen 按“服务商默认 + 单模型覆盖”解析 `thinking_budget`：官方 Model Studio 的非 Coder Qwen3 自动使用保守三档，第三方网关默认只显示开关，显式配置预算后才亮档位菜单；新增服务商必须用真实请求确认其是否透传该字段。**思考档位的三个派生位一律以 Pi 的 `thinkingLevelMap` 为准、renderer 不复刻判定**：`supportsEffortDial`（有没有档位菜单）、`thinkingAlwaysOn`（`off === null` 即思考关不掉，不画「不思考」那一行）、`thinkingLevels`（真正有哪几档——GLM-5.3 是 low/high/max，没有「中」、多一个「最高」）。目录条目没写某个 compat 位时要按 Pi 的 `detectCompat` 兜底，「字段缺席」不等于「不支持」（2026-08-21 实案见第二层 GLM-5.3 那条） |
| 服务商实体化（configVersion 2：modelProviders + providerId 挂接 + resolvePresetConfig 解析视图 + loadConfig 惰性迁移带 .bak） | `config-manager.ts` | 数据治理——连接信息单一事实源（换 key 一处生效、9 预设去重成 7 实体实测）；红线附带修复：内置 .env 连接信息与模型名此前经预设编辑路径明文可见，现 IPC 边界拒发 + 掩码（短 key 全遮防"前6后4"整段泄露） |
| voice-tool-truncate 结果保真 | `voice-tool-truncate.ts` | 语音信道的物理带宽约束 |
| 随消息图片落盘 + 路径事实注入（官方 uploads/ 形状） | `chat-uploads.ts` / `pi-agent-service.ts` / `dcRuntime.ts` / `dc-headless.ts` / `dc-export.ts` | 信息缺口非能力问题——粘贴图只进视觉上下文时磁盘上无文件可引用，模型再完美也无法引用不存在的文件（2026-07-22 实案：找图 → 全盘 find → TCC 连环弹窗）。落盘 `artifacts/<conv>/uploads/pasted-<ts>-<i>.png` + 消息注入路径事实；srcdoc 预览 data URI 内联、headless 导出内联、交付包随包拷贝，四条消费路径同源 |
| 主目录/全盘遍历需确认（`detectHomeWideScan`） | `pi-security.ts` | 隐私暴露面治理——读操作无破坏性但 `find ~` 触达桌面/iCloud/照片全域（含 TCC 连环弹窗）；沙箱只管写与网络不管读隐私，故不因沙箱降级。明确子目录不拦；永久（隐私边界与模型能力无关） |
| 角色辅助文件 seed hash 自愈（`seedRoleExtraFiles`） | `role-loader.ts` | 升级通路/数据治理——此前"存在即跳过"导致 preflow.json 等辅助文件的种子更新永远到不了存量安装（2026-07-22 模板扩展实案：新模板选项无法下发）。与 agent.md 同款语义：未被用户改过（hash 对账）→ 自动升级；改过 → 用户权威；无 hash 的存量文件内容恰好等于种子 → 补记 hash 打通后续通路 |
| API key 按请求显式注入（`withSessionStreamOptions`〔原 withSessionApiKey，2026-07-23 扩展为通用会话流选项注入器,兼管 cacheRetention〕 + 三处 completeSimple 显式 apiKey） | `config-manager.ts` / `pi-agent-service.ts` / `subagent-runner.ts` / `evolver-agent.ts` / `agent-dreamer.ts` / `agent-extractor.ts` / `memory-store.ts` | 多租户凭证正确性——process.env 是进程级单例，与"同进程多会话各持 key"结构性互斥（会话专属模型上线后并发互踩 401 实案）。契约依据：pi-ai 公开 API `StreamOptions.apiKey` 显式优先于 env（0.74→0.80.10 逐字核验稳定；0.80 新 api 层仅认 options.apiKey 且新增 ProviderEnv 请求级作用域——上游同向演进）。安全审计（2026-07-21）：key 流向仅 `createClient→Authorization 头`，全 provider 源码零日志涉键，onPayload 观测钩子只见请求体见不到头；对比 env 承载反而收窄暴露面（env 会被 agent 的 bash 子进程继承）。分发核验：安装包对本机全部 8 个密钥指纹 0 命中、无 .env 随包（electron-builder 排除项生效）。env 仍设置仅作未覆盖路径兜底 |
| session-artifacts 清单带"最后真实修改"（磁盘 mtime） | `pi-agent-service.ts` / `artifact-store.ts` | 外部状态可观测性——模型无法凭上下文知道工具调用是否真的落了盘；"声称改过 vs 磁盘事实"可对账（2026-07 叙述式完成实案的证据面，配合第二层"宣布后停手"催促） |
| 开源发行分类门闩（策略即清单 + 裁剪脚本 + fail-closed 校验器） | `config/open-source-policy.json` / `scripts/make-open-source-cut.mjs` / `scripts/verify-open-source-candidate.mjs` / `tests/unit/open-source-candidate.test.ts` | 数据治理——公开仓库是脚本从私仓**确定性裁**出来的，不是手工维护的分支，所以必须有东西保证「校验器认为不能公开的」与「实际没被拷进去的」不可能对不上。套判定公式：模型再完美也不知道所有者愿不愿意公开某个文件，这是意图缺口不是能力缺口，永久。机制：每条路径由唯一一条终局规则分类（规则按序求值，最后命中者决定去留），未分类即 fail；`exclude` / `blocked-replacement` 的材料漏进发行树即 fail；规则重叠必须显式 `overrides` 声明；策略必须对着它声称的评审基线写（`pathSetSha256`）。校验器只读已提交的 git 对象（`evidenceOnly`），工作区脏不脏都不影响定论。**发行前的人工环节走清单对齐，不写进策略文件**：`git fetch <公开仓库> main` 后 `git diff --stat FETCH_HEAD <新候选>`，把「这次多了/改了哪些文件」摊给所有者看，点头才推。 **2026-08-13 分层降级（A → B）**：本机制原先还带一层签字（每条规则一个 owner/approver/APPROVED，绑定候选树内容哈希，改一字节则全部失效）。降级原因不是判层错了——数据治理确实属永久架构——而是**没判代价**：签字那一半的价值前提是「人真的逐条看过 750+ 个文件」，实际不会发生，于是防护力接近零而成本是每次改动全部作废重来。同一天还暴露过它的失效模式：agent 改了 6 个文件后用「重新钉哈希」把失效的签字重新激活，因为私仓那份策略带着 APPROVED、且可被无限次重裁，等于一张空白支票。真正持续在防的是分类（抓到过官网被误归为"可发行"、抓到过 22 个没人看过的新文件），而分类是免费的。所以保留分类、移除签字：`review` 块、`candidate` 批准字段、`candidateContentSha256` 与相关的 5 类错误码一并删除，策略加 `classificationOnly: true` 声明它不承载任何人的批准。**教训（比这条机制本身更通用）**：「这机制该不该永久存在」和「这机制值不值这个代价」是两个独立问题，可以一个是"是"一个是"否"；第二问的答案该由付代价的人回答。出生规范因此补一条见文末。退出条件：无，永久（分类这一半） **2026-08-17 补一条使用陷阱**：校验器的 `--commit` 要的是**裁剪出来的公开树的提交**，不是私仓 HEAD。对着私仓跑必然 FAIL 且毫无信息量——该裁掉的东西当然还在，三个 `*_PRESENT` 错误码按定义全响。`docs/release-acceptance.md` 当时把 `OPENPIPAL_CANDIDATE` 定义成 `git rev-parse HEAD`（对 A–E 那几道 macOS 闸门是对的，对 F-00 这道是错的），同日已修正并在 `docs/open-source-readiness.md` 补了候选提交的构建步骤。实用推论：**报告要分两层读**——`RULE_OVERLAP_CONFLICT` 与 `UNKNOWN_CANDIDATE_PATH` 是分类缺陷，在哪棵树上都必须为零，因此可以拿私仓当廉价预检；三个 `*_PRESENT` 只在候选树上有意义。 **2026-08-17 首次真跑到 PASS，又补两个坑**：① **验证的那个仓库必须同时够得着候选提交与 `reviewedBaselineCommit`**。基线只在私仓里，所以「在候选目录 git init 一个新仓库」是错的——基线路径集算出来是空的，所有基线域规则匹配不到任何东西，报一堆 `UNKNOWN_CANDIDATE_PATH` 与 `PATH_SET_HASH_MISMATCH`，而这个失败与候选本身无关。正解是克隆公开仓（保历史）+ `git remote add priv <私仓>/.git` 取基线提交。实测：645 个错误 → 14 个。② **台账行的散文限定语必须配一个机器可读的双胞胎**。解析器只读反引号里的路径，把 `resources/skills/*` 这类行整组当排除，读不懂后面的「除了……」。收窄台账行而不同步加 `ledgerExceptions`，必然 `LEDGER_EXCLUDED_PATH_PRESENT`。 三次纠正分处三层——规则重叠未声明（改策略时漏的）、验错了树（文档写反的）、基线不可达 + 限定语没机器化（流程从没跑通过）——**第三层只有真跑一次才会撞上**，因为它要求同时具备两个只存在于不同仓库里的东西。 |
| artifact 写入对账门闩（`lastKnownMtimeMs` 基线 + `evaluateArtifactWriteGuard`） | `artifact-registry.ts` / `pi-tools.ts` | 数据治理/竞态——用户经 UI 直改 artifact 落盘后，`create_artifact` 同 id 全量重发从不读盘会静默覆盖用户的修改；套判定公式：模型再完美也不知道用户在它两轮之间改了磁盘，这是外部状态可观测性缺口，不是模型能力问题，永久成立。机制：`upsert`/`read_artifact`/`edit_artifact` 成功后把磁盘 mtime 记为基线，`create_artifact` 写入前对账磁盘当前 mtime 是否领先基线超过 1.5s 容差，领先则拒绝并指路 read_artifact（读/改后基线刷新，循环收敛）。**diff 证据链（2026-07-21 补全下半程）**：mtime 只能证明"被改过"，模型上下文里未必有它自己上一版原文，光靠 read 全文无从识别哪些字是用户改的（信息缺口非能力问题——2026-07-20 真机实测：模型 read 后重做仍丢用户改的标题）。注册表另存 `lastAgentContent` 快照（create/edit 写出后刷新、read 不动），read_artifact 发现磁盘与快照不一致时把 `buildExternalEditEvidence` 算出的 diff 附进读取结果——保不保留、怎么整合由模型判断（确定性归代码，判断力归模型）。2026-07-21 真机验收（生产 app + qwen 主档）：证据必达有铁证（无历史单轮行为探针，模型精确复述用户直改的改前/改后，唯一信息源即 diff 块）；行为面实证=风格重做保住了用户改的标题语义（此前同场景标题被整个丢弃），弱档丢"明显像脚手架"的标记后缀属合理裁量——diff 是证据不是命令，符合设计意图。已知限制：注册表是内存态，应用重启后首个回合无基线不设防、无快照不出 diff——可接受，重启后模型上下文里通常也没有旧内容可覆盖，谈不上"覆盖用户修改"。退出条件：无，永久（`artifact:save` 这条 UI 直改路径本就是意料之中的外部写入源，不是待消灭的过渡态）。 |
| git 身份护栏（`user.useConfigOnly=true` 随每条命令注入） | `openpipal-execution-env.ts`（`buildGitIdentityGuardEnv`） | 外部状态可观测性——用户没配 `user.name`/`user.email` 时，git **不报错**：拿登录名 + 机器名拼一个身份盖上去，**退出码 0**，警告还印在提交*之后*（2026-08-24 实测 git 2.50.1，作者落成 `alice <alice@Alices-MacBook-Pro.local>`）。于是所有基于退出码的纪律都拦不住；更糟的是编码助手提示词里那句「每条命令都要看退出码」**反而把模型引开了**——退出码是 0，按我们自己的规矩它就该往下走。真机验收实案：模型一路「成功」地把假署名写进历史，回话只有「README 已更新，现在提交」，全程无人察觉。套判定公式：**再完美的模型也不知道用户叫什么、邮箱是什么，缺的是数据不是判断力**，永久成立。机制：把无声的失败变成有声的失败（退出码 128 + git 原样的配置指引），转告用户这一步交给模型，**不为此加提示词**。用户已配身份时是 no-op（实测退出码 0、作者正确，零误伤）；用户自己显式设过 `user.useConfigOnly` 就不插手。索引要排在凭据桥之前算——两者都按 `GIT_CONFIG_COUNT` 顺延写位。回归在 `git-policy.test.ts`（真跑 git，验行为不验字符串）。**落地后真机复测（2026-08-24，deepseek-v4-flash-vision-exp，同一场景 n=6）**：git 如期退出码非 0。**告知率 6/6**；**默认行为是停下来问用户（4/6 一条都没提交）**；提交的 2 次里，1 次沿用了仓库里上一条提交的作者（合理推断，不算编造），1 次自己拼了 `alice <alice@local>`——但那次也如实告知并主动提出可以 amend，且都发生在「最后一条、还没推」这个最便宜的时刻。**结论：不为此加提示词。**曾提过在纪律里补一句「署名不替用户编」，跑完数据后撤回——那时只有 n=1，属于没依据的建议。已知边界：单一模型、单一任务措辞、全部未推送 |
| dotenv **模板**在凭据边界上放行读（`.env.example` / `.env.sample` / `.env.template` / `.env.dist`） | `credential-paths.ts`（`isEnvTemplateBasename` + `buildEnvTemplateReadAllowGlobs`） / `pi-security.ts`（`isSensitivePath`）/ `sandbox-manager.ts`（`allowRead`） | 外部环境可用性——**这是一次收窄过头的纠正，不是新拐杖**。原来的宽 glob `/**/.[eE][nN][vV]*` 同时进 `denyRead` 与 `denyWrite`，把按惯例提交进版本库、只放占位值的模板一起拦了。挡住的不是读密钥，是 `git stash` / `git status` / `git checkout`——git 要把工作树打成 tree 就得读每个被跟踪的文件，读不了整条命令就失败。**代价量过**：影子运行 60 次里 19 次撞上 `.env.example` 的 `Operation not permitted`，横跨 10 道题；实案 `6919551@r1` 想 `git stash` 回基线做对照，被自家护栏挡住，原话「stash 尝试因 `.env.example` 权限问题未执行任何回退」。**安全收益本来就很浅**：这些文件已经在 git 里，助手照样能 `git show HEAD:.env.example` 读到内容（`.git` 不在读拒表里），所以宽 glob 对被跟踪的模板基本是安全戏。套判定公式：模型再完美也读不了一个被我们自己挡住的文件，缺的是环境不是判断力，**永久**。三条克制：① 只列四个通用约定（不含 `.env.test` / `.env.defaults` 这类可能真放值的）；② **只放读不放写**——SRT 的语义是 `allowRead` 压过 `denyRead`、而 `denyWrite` 压过 `allowWrite`，所以写那一半原样保留，拒读拒写规则一个字没改；③ **逐字节比、只认小写**，`.ENV.EXAMPLE` 继续拦——沙箱那层是 glob，做不出大小写不敏感的例外，应用层要是折叠就会出现「`read` 工具能读、bash 读不到」的分裂，两层不一致比严一点更糟（这条是新加的用例当场照红逼出来的）。修后同语料 15 次 0 次复现。**已知放宽面**：有人把真密钥误写进 `.env.example` 时，助手现在读得到——改回去只需删 `allowRead` 那一行。回归在 `credential-read-boundary.test.ts`（真密钥仍 risky / 模板放行 / 大写变体仍拦，三件事同一条用例钉住） |
| 沙箱下给 git 的 dotenv 排除清单（`core.excludesFile` 随每条命令注入） | `credential-paths.ts`（`buildGitDotenvExcludeBody`）/ `sandbox-manager.ts`（`getGitDotenvExcludesFile`）/ `openpipal-execution-env.ts`（`buildGitDotenvExcludeEnv`） | 外部环境可用性——**病灶不是"读密钥"，是"遍历"**。SRT 把 `denyRead` 一律翻成 Seatbelt 的 `(deny file-read* …)`，而 `file-read*` 连 `file-read-metadata` 一起拒，于是 `lstat(".env")` 也回 EPERM；git 走工作树必须 lstat 每个条目，撞上就整条命令挂掉。立案原文即 `error: lstat(".env"): Operation not permitted`。套判定公式：模型再完美也没法让 git stat 一个被 OS 拒掉的文件，缺的是环境不是判断力，**永久**。**五种仓库形态 × 9 条 git 命令 × 三个候选方案的实测对照（2026-08-28，生产配置、真沙箱、都走 SRT 自己的 `wrapWithSandbox`）**：① dotenv 已被 gitignore（正确做法，最常见）——**今天本来就全绿**，三个方案都不用管；② dotenv 未被忽略也未被 track——`git stash -u` / `git add -A .` 挂，**本清单修好，且只有本清单修得好**；③ dotenv 已被提交进版本库（含"刚 clone 出来"）——三个方案都修不好 `git stash`。**被否掉的方案要写下来，免得再发明一遍**：曾准备往 SRT 生成的 profile 里插一条 `(allow file-read-metadata (regex …))` 放行元数据（内容仍拒）。它在形态②一条都修不了；在形态③的"索引已安顿"状态下能修好 stash，但在**刚 clone 出来**这个助手真实的入场状态下收益归零（index 与文件 mtime 同秒，git 落进 racily-clean 判定必须重读内容），反而把 `git diff` / `git add -A` / `git commit -am` 从退出码 0 变成 128——**净负收益**，加上它只能靠对 `sandbox-exec -p "…"` 那段字符串做手术（SRT 不导出 profile 生成器、配置 schema 里也没有塞原始规则的口子），所以不采纳。**这一版沙箱规则一个字没动**，`cat`/`grep`/`python open()` 该拒的照拒。三条克制：① 四个模板名逐条 `!` 放回来——它们读得到，排掉反而让新建的 `.env.example` 对 `git add` 隐身；② 用户自己设过 `core.excludesFile` 就完全让路（`GIT_CONFIG_*` 后写的赢，我们再追一条等于替他改主意）；③ 清单文件把用户 `~/.config/git/ignore` 的内容拼在前面——`core.excludesFile` 是整体替换不是叠加，直接换掉会把用户的 `.DS_Store` 之类规则全丢了。**副作用（是我们要的方向）**：`git add -A` 从"整条失败"变成"静默跳过那个文件"——它本来也提交不了（git 读不到内容），跳过一个文件好过让整条命令挂掉。**记一笔与本机制无关的既有暴露面**：dotenv 一旦**被提交进版本库**，`git show HEAD:<dotenv>` / `git log -p --all` / `git grep <secret> HEAD` 都能读出内容（`.git` 不在读拒表里，也没法拒——拒了 git 就废了），改前改后一模一样。也就是说对那种仓库，工作树那份拒读本来就是安全戏。回归在 `credential-read-boundary.test.ts`（真沙箱里改动前后各量一次，不对照分不清"本来就绿"和"被修好了"）。日落条件：dotenv 的宽拒读规则不再覆盖 metadata（SRT 支持 `file-read-data` 粒度，或我们换掉沙箱实现）时删掉 |
| 登录 shell 的 PATH 补齐（启动时问一次，合并进 `process.env.PATH`） | `login-shell-path.ts`；在 `index.ts` 的 `whenReady` 开头发车，MCP 那条后台链显式等它 | 外部环境可用性——macOS 上**双击图标启动**的进程继承的是 launchd 那份最小 PATH。2026-08-25 实测（用一个最小 `.app` 分别经 `open` 和 Finder 启动做对照）：Finder 拉起的进程只有 `PATH=/usr/bin:/bin:/usr/sbin:/sbin`，node / npm / uv / pnpm / cargo / go 全部 command not found，只有 git 和 python3 因为躺在 `/usr/bin` 才活着。受害的有三处：编码助手的 bash 工具（`openpipal-execution-env.ts` 把 `process.env` 透传给子进程）、stdio MCP server（`mcp-manager.ts` 同样透传，命令常写成 `npx`）、以及 **OS 级沙箱**（`@anthropic-ai/sandbox-runtime` 要在 PATH 里找 `rg`，`/opt/homebrew/bin/rg` 不在 launchd 那份里，找不到就整段降级成应用层安全模型——一个安全边界被无声关掉）。**这个洞对现有测试完全隐形**：`npm run dev` 和 Playwright 都从终端起进程、继承完整 PATH。套判定公式：模型再完美也变不出一个不在 PATH 里的 npm，缺的是环境不是判断力，**永久架构**。机制：起 `$SHELL -l -i -c`，用 `__OPENPIPAL_PATH_BEGIN__` / `__END__` 标记把 PATH 从 rc 文件的噪声里夹出来，登录那份排前面去重合并；探针失败一律保持现状（顶多和今天一样坏，不会更坏）。**两档降级是实测逼出来的**：交互登录 zsh 本机空载 0.78s，满载跑单测时撞满 5s 上限返回 null——那一刻用户白拿一个断的 PATH；所以第二档退回 `-l -c`（实测 0.01s，快 78 倍，本机同样够用）。仍要带 `-i` 做第一档，因为 nvm 官方安装脚本默认只写 `.zshrc`（交互档），而 node 恰好是最需要补的那个。`stdio[0]` 必须 `ignore`：交互 shell 拿到开着的 stdin 会坐等输入永不退出。**发车与等待的位置是量出来的**：探针在 `whenReady` 开头发车、不 await（拖首屏不值得）；沙箱那一处必须等，因为实测双击启动的时间线是沙箱 0.386s 就跑完、探针 0.554s 才回来，不等于白补——但只等 2s 封顶，探针最坏走到第二档、把首屏卡十几秒比沙箱降级更糟，超时就维持今天的行为。MCP 那条后台链无限等（它本来就在窗口之后）。修完实测：沙箱从「初始化失败，降级到应用层安全模型」变成「沙箱已启用（macOS Seatbelt）」，首屏与 HTTP 就绪时刻均无可测退化。退出条件：无，永久 |

## 第二层：能力拐杖（为当前弱模型建，到期必拆）

日落触发是**能力实证，不是日历**：换上某档模型后实测该行为可靠，才拆。
细分两种：**零成本型**（输入干净时是 no-op，不限制模型）可长期保留；**拒绝式/约束式**（reject、强制步骤、自动干预）有误伤面，模型越强误伤越多，必须日落。

| 机制 | 类型 | 存在理由 | 日落条件（能力实证） | 退出方式 | 误伤面 |
|---|---|---|---|---|---|
| legacy Runtime 回滚阀门（`OPENPIPAL_AGENT_RUNTIME=legacy` 保留旧实现整条路径） | 零成本型（不选就走不到，对默认路径零影响） | 2026-08-10 默认已切 pi-core（20/20 受控样本 + 两家服务商连续性矩阵各一次通过）。但曾出现一次**至今未归因**的 legacy 静默挂起，根因未明前需要一个改一个环境变量就能退回去的通道 | ①pi-core 作为默认稳定运行一个观察期且零不可归因挂起，**且** ②那次挂起的根因已查明或已被证明缓解 | 按 `docs/architecture/runtime-v2-migration.md` 的删除计划执行；**先拆 `pi-tools.ts` 与语音管线的耦合**（它表面是 legacy 门面，实际 `realtime-tool-bridge.ts` 在用，机械删会坏语音且编译期无信号） | 低（不选就不触发；代价是每次产品改动要在两个 Runtime 各改一遍，已有多次实例） |
| 动画门闩（taskType 绑定 / anti-inline / 截断检测 / 重复创建去重） | 拒绝式 | qwen3.6/3.7 无视模板选择、内联引擎源码、截断落盘 | 模型可靠按 roleBrief.taskType 产出且不内联 | 已有逃生舱 `<!-- non-anim -->`；整体拆除删 `pi-tools.ts` 门闩块 | 中（正当的非动画首产物需声明逃逸） |
| 文件工具改道提示（`fileToolHint`：execute_code 的代码命中纯文件读写特征 → 结果尾部附一行"本机有 read/grep/edit/write"） | 零成本型（只附文本，不拦不改行为） | 模型有 grep/read/edit 却用 `open(路径).read()` + 正则找内容（2026-07-29 design 实案）。绕道三笔代价：多一次用户确认（沙箱未启用时 execute_code 每次要点允许）、整篇文件进上下文、产物脱离 artifact 管线 | 连续多轮 execute_code 零命中该正则（模型自发优先选文件工具） | 删 `file-tool-hint.ts` 与 `pi-tools.ts` 里那一处拼接 | 极低（只是多一行提示；`open()` 为真计算而读时提示无害，模型可无视） |
| 回执门闩（artifact 三工具 + write/edit 文件通道 + execute_code 代码通道，拒绝 `[内容已保存…]` 占位入正文） | 拒绝式 | 模型把上下文压缩回执当正文复制：11KB 场景被覆写成 73 字节（2026-07 实案 ①）；write 文件通道漏拦致 bow-os 设计 token ×2 + 教案 21552 字符被覆写（2026-07-26 实案 ②，教案不可恢复）；write 被拦后模型改走 execute_code python 写文件（实案 ③，同日） | 模型可靠区分回执与正文（换强模型后跑改稿回归） | 删 `pi-tools.ts` 三处 + `pi-security.ts` classifyToolRisk 两处 `containsReceiptPlaceholder` 检查；识别函数保留（UI 损坏态提示复用） | 极低（正文合法包含该串≈不可能；edit old_string/oldText 已豁免供修复）。若门闩生效后仍出现回执落盘，即证明存在执行侧重放路径，需另查 |
| questions_v2 宽容解析（schema Union + 叶子元素收敛） | 零成本 | 弱模型 schema 偏差双向（结构发扁/叶子发胖），曾致 React #31 白屏 | 无需日落——干净输入零成本，属 Postel 法则 | — | 零 |
| 逐屏文本摘要自检（代替视觉核对） | 能力位门控 | qwen3.7-max 无视觉 + photon WASM 失效 | **自动**：`supportsImages !== false` 时截图直接入工具结果 | 换视觉模型即自动升级，零改动 | 零 |
| advisor 档位（执行弱模型 + 工具调强模型问建议） | 经济性 | 成本套利：大部分 token 走便宜档 | 执行档自身判断力足够时降为可选 | 删 `~/.openpipal/subagents/advisor.md` 一个文件 | 低（多一跳延迟） |
| 流式残片取证 ring buffer（`window.__dcStreamDebug`，dev-only） | 诊断拐杖 | 流式期"顶部裸属性文本"残片未逐字节归因（实验已证伪残 tag 直漏假设），需真实复现样本定罪 | 残片根因定罪并修复后即拆 | 删 `HtmlPreview.tsx` 的 `recordStreamDebug` 及三处调用 | 零（DEV 构建外是 no-op） |
| design 角色 memory: off | 文件式开关 | 记忆抽取把任务过程存成跨会话偏好（污染实案） | 记忆系统治理完成（抽取质量可信） | 删 agent.md 一行 frontmatter | 低（失去跨会话个性化） |
| SKILL_USAGE_NUDGE（技能索引尾部催促"匹配就先 read SKILL.md"） | 零成本 | 官方 docs 原话 "models don't always do this"——弱模型见索引不读正文（Pi 研究时发现的漏登记拐杖，2026-07 补记） | 无需日落——纯提示词一句，强模型本就会读，不构成约束 | 删 `skill-manager.ts` SKILL_USAGE_NUDGE 常量即退 | 零 |
| 空完成盲目续跑（thinking-only/空 content 时注入一条内部续跑提示重试一次；2026-08-06 补登记——出生时漏登） | 干预式 | 弱模型/部分网关正常收流但只回思考不回正文（流式抖动或"只想不说"）；pi agent-loop 对此完全不处理直接收尾 | 换强模型后连续多轮零触发（`[Pi] 检测到空完成` 日志计数） | 删 `empty-completion-guard.ts` 的续跑提示分支（溢出分型与自愈属第一层，保留）；调用点不动 | 低（重试前已确认无正文无工具调用，不会重复执行操作；2026-08-06 起溢出型失败已分流，不再误吃这药） |
| GLM-5.3 档位表补丁（`patchStaleGlmThinkingLevels`：zai 方言 + 5.3 以上 + Pi 没写档位表时，补 `{off:null, low, high, max}` 并开 `supportsReasoningEffort`） | 数据缺口型（不是模型能力拐杖：补的是**目录数据**的空白） | Pi/models.dev 对 GLM-5.3 的思考数据只在 opencode-go 那条写对了，`zai` 直连那条仍是 `thinkingLevelMap: null` + `supportsReasoningEffort: false`。照它发有两个错：用户关思考时发出 `thinking:{type:'disabled'}`——官方文档明写 5.3 起不支持关思考、请求会失败；开着思考又一个档位字段都不发，服务端按默认 **max** 猛想（2026-08-21 实案：一轮思考 111 秒、0 字正文，随后被中继掐断） | Pi 的 `zai/glm-5.3` 条目补上 `thinkingLevelMap` + `supportsReasoningEffort`（升级 pi 后跑 `tests/unit/thinking-effort-dial.test.ts` 里 5.3 那两条即知） | 删 `config-manager.ts` 的 `patchStaleGlmThinkingLevels` + `GLM_ALWAYS_THINKING_LEVEL_MAP` + `zaiThinkingAlwaysOn`；三处调用点还原成直接 return | 低（只作用于 zai 方言下 5.3 以上、且 Pi 自己没写表的条目；Pi 写了表就以 Pi 为准，不覆盖） |

## 已拆除（拆过的别再发明一遍）

按出生规范，拐杖的日落条件是**出生时就登记好的**。这一节记的是**真的按那条规则拆掉的**机制，
连同当时的数据。留着是为了防止同一个想法被重新发明——想加回来，先拿出比这里更强的证据。

| 机制 | 拆除日期 | 预先登记的判定规则 | 实测结果 | 代码在哪 |
|---|---|---|---|---|
| **收工闸**（`agent-runtime/completion-gate.ts` + `pi-core-runtime.ts` 外层循环）——模型不再调工具时，把本轮工作区净改动摆到它面前一次 | 2026-08-28 | 「开臂的『补过一次自动确认之后仍然零改动交付』要显著低于关臂。**若两臂量不出差别就当场拆除。**」外加一条护栏：「『触发次数是 0』单独不构成拆除理由——必须先看三态分解，只有当闸门**真执行到了**的样本足够多、其中触发仍是 0 时才算数」 | **两条护栏都过了，主判据量不出差别。** 3 轮 × 7 题 × 开关两臂 = 42 次运行，剔掉 3 次上游打断剩 39 次有效。**三态分解干净**：开臂 21/21 全部「触发」、关臂 21/21 全部无日志（逃生舱口生效），**一次「未执行」都没有**——上一轮那个「6/7 没执行到」的仪器故障已经修好，这次的 0 是真 0 不是空数。主判据「零改动交付」：关 1/18 vs 开 0/21，基础率 2.6%，量不出差别。次判据「解决率」：关 3/18 vs 开 5/21，配对符号检验 18 对里 3 对不一致（开赢 2、关赢 1），p≈1.0；剔掉 4 道判据不可解的题重算后 7 对配对格里开赢 1、关赢 1、打平 5——正反面。**代价那一栏当初估错了**：写的是「低：只多一轮往返」，那是按「偶尔才响」估的；实际 21/21 每次都响，r2/r3 的事件数中位数开臂比关臂多 10~11%（6884→7871、6706→7439），墙钟无差别。于是这道闸是**每一次编码任务都多花一成模型调用、换不到可测收益**。**当初的立论本身也已作废**：唯一那条实测证据（「零改动交付且不说自己没动手」）事后查明是模型在按项目 CLAUDE.md「修前写三点等确认」等人拍板，而无人值守的评测永远不答那句话——那是测量假象，不是收工太早 | `git log -- src/main/agent-runtime/completion-gate.ts`；对照实验的编排与报表仍留在 `tests/bench/gate_ab.sh` / `gate_report.js`（下一个机制要做 A/B 时照抄这套） |
| **「宣布后停手」活性催促**（曾登记为「已确认待实现」，从未落地） | 2026-08-28 | 无——它当初写的是「出生即带：仅弱模型档位启用」，没登记证伪条件 | **不实现，直接划掉。** 它和收工闸是同一个假设的两种写法（模型宣布干完但没真干活，需要外力推一把），而收工闸把这个假设**真的测了**：21/21 触发、零可测收益。在有更强证据之前，再实现一个同假设的干预式拐杖没有依据；而且它的误伤面（登记为「中：误判正常文本回答为空转」）比收工闸的「低」还高。想重提，先拿出「announce-then-stop 真实发生率」的实测数字 | 从未实现 |

## 第三层：提示词脚手架（随模型档位可换，最先拆）

步骤脚本对强模型是降智约束。全部走文件式（`system-agents/<role>/agent.md`、`skills/*/SKILL.md`），按角色/按模型替换，不涉及代码。

| 脚手架 | 位置 | 说明 |
|---|---|---|
| design 步骤绑定（步骤1 taskType 绑定、步骤8 核对文本摘要） | `system-agents/design/agent.md`（seed 在 `role-manager.ts`，seedhash 保护） | 弱模型需要显式步骤；强模型给目标+证据即可 |
| 全局替换 grep 纪律、逐幕 edit 骨架 | 同上 + `skills/animation-basics/SKILL.md` | 防弱模型整文件重写/一次性长产出截断 |
| SKILL.md 间距硬规则（8px 系统/abs≥16px） | 四份设计类 SKILL.md | 强模型有设计判断力后可放宽为参考值 |
| VOICE_ETIQUETTE | `realtime-session.ts` | 语音场景礼仪，偏永久（信道特性非模型能力） |
| **编码验证纪律四条**（通用解不许开特判 / 不许改已有测试期望 / 判回归要比集合不许抽样 / 技能与项目文档写明"先征得同意"时不适用"直接做"） | `roles/coding-role.ts`（seed 到 `system-agents/coding/agent.md`，seedhash 自愈；文件式，用户可覆盖可删） | **问题是量出来的、对策没被证实**（2026-08-28）。问题侧 n=60：模型 41 次宣称"无新增失败"，去掉判分器噪声后仍有 10 次是错的（24%），最贵一次抽查 4 个文件就宣布无回归，实际打红 23 条。对策侧做过一次前后对照（同 5 道语料各 15 次，`tests/bench/cap_run.sh`）：真做全集对照 **9/15 → 7/15，方向还反了**——诚实的说法是"**无效果证据**，且这个样本量本来就抓不到中等幅度的变化"，不是"已证明没用"。**日落条件**：下一次有更大样本时**优先重测这一条**；若届时仍量不出差别，且问题基础率随模型升级降到 10% 以下，就整块删掉。**退出方式**：文件式——删 `system-agents/coding/agent.md` 里对应几行即可，代码路径不涉及。**误伤面**：多约 250 字符提示词；"不许改已有测试期望"在少数确实该改期望的题上会让模型多说一句请示 |

## 出生规范（新增机制必须遵守）

1. **先判层**：用判定公式定层级；拐杖必须在本台账登记（表格加一行），声明日落条件、退出方式、误伤面。
2. **优先证据式，慎用拒绝式**：给模型事实（文本摘要、lint 结果、截图）让它自己判断——证据式随模型智能升值；硬 reject 只用于**不可逆或高代价错误**（截断产物落盘、敏感路径写入）。
3. **拐杖必须带退出机制**，优先级：能力位门控（自动退场）> 逃生舱口（模型可声明穿过）> 文件式开关（删文件即退）> 硬编码（禁止——必须人拆的拐杖不许进主干）。
4. **升级模型时的例行动作**：翻本台账第二、三层，逐项做能力实证，满足日落条件的当场拆。
5. **分层之外还要判代价**：「这机制该不该永久存在」和「这机制值不值这个代价」是两个独立问题，可以一个答"是"一个答"否"（2026-08-13 开源签字门闩实案：数据治理确实属永久架构，但签字那一半的价值前提是"人真的逐条看过 750+ 个文件"，不会发生，于是防护力≈0 而成本是每改一字节全部作废）。**第二问的答案由付代价的人回答，不由 agent 替他回答**——尤其当这机制的作用正是约束 agent 自己时。
