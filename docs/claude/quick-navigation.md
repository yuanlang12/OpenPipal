# Quick Navigation —— 按 bug 类型定位文件

> 修 bug / 改子系统前先查本表，不要从根目录漫读。新 bug 类型的定位路径加到本表。

| Bug 类型 | 首先看 | 然后看 |
|---|---|---|
| 消息顺序/重复/丢失 | `stores/chatStore.ts`（事件处理） | `main/pi-event-adapter.ts`（事件适配） |
| dc 设计稿渲染/参数面板/导出（`.dc.html`） | `renderer/components/artifacts/dcRuntime.ts`（检测+内联+overrides 读写）、`HtmlPreview.tsx`（`__dc_booted`/`dc:set-props` 桥） | `main/dc-export.ts`（离线导出装配）、`resources/dc-runtime/`（support.js 冻结 ABI + React vendor）、`main/pi-tools.ts` create_artifact 的 dc 门闩 |
| mp4/pptx/handoff 导出、Agent 导出工具 | `main/dc-video-export.ts`（seek 协议逐帧）、`main/dc-pptx-export.ts`（deck 逐页截图+OOXML，`captureDeckStageFrames` 供 handoff 复用）、`main/dc-handoff-export.ts`（交接包：HANDOFF.md+design+reference+tokens） | `main/dc-capture.ts`（共享 CDP 捕获层）、`main/pi-tools.ts` export_artifact、`main/export-artifact-validate.ts`（格式门槛+校验文本纯函数） |
| 首启引导（全屏四步 / Mark 引导动线） | `components/OnboardingOverlay.tsx`（步骤/插画/Mark 落点，落点约定：永远压在白色 mock 窗口上） | `main/config-manager.ts` `onboardingCompleted`、`ipc-handlers.ts` `onboarding:*`；E2E 截图 `tests/e2e/onboarding.spec.ts` |
| 消息渲染/样式 | `components/MessageBubble.tsx` | `components/messages/*.tsx`（各类卡片） |
| 模型说到一半断了 / `Stream ended without finish_reason` / 界面出现「正在重试 (n/m)」 | `main/isolated-stream-signal.ts`（重连闸门：`isRetryableAssistantError` 判据 + 5 次 + 5 分钟窗 + 吞掉重连那次的 `start`）；先看日志里是 `outcome:"provider_error"` 还是 `"watchdog"`——后者才是我们的 300 秒静默看门狗（`stall-watchdog.ts`，按事件续命、工具期撤弦，不是总时长上限） | `main/agent-runtime/pi-core-runtime.ts` + `main/pi-agent-service.ts` 的 `recordStreamRetry`（推 `stream_retry` 事件、`adapter.reset()`、给看门狗续命）→ `stores/chatStore.ts` `onStreamRetry`（丢弃半截思考/正文 + 插提示）。**改这条链必须记住重连是整轮重发不是断点续传**，细节见 `mechanism-registry.md` 第一层那行 |
| Thinking 显示异常 | `stores/chatStore.ts`（onThinking/onThinkingEnd） | `MessageBubble.tsx`（ThinkingMessageCard） |
| 流式预览/可视化 | `components/StreamingInlinePreview.tsx` | `stores/visualizerStore.ts` |
| 工具调用结果 | `messages/ToolCallCard.tsx` | `chatStore.ts`（onToolStart/onToolEnd） |
| IPC 事件不到达 | `main/ipc-handlers.ts`（事件路由） | `preload/index.ts`（IPC bridge） |
| SSE/浏览器模式 | `web-api-shim.ts`（SSE 客户端） | `main/http-server.ts`（SSE 服务端） |
| AI 工具选择/安全 | `main/pi-tools.ts` | `main/pi-security.ts` |
| 编辑器（Zed/JetBrains）里工具卡住不动、授权框跑到桌面端 | 桌面侧 `main/http-server.ts` `writePermissionToStream`（活动流按 conversationId 索引，extension/acp 都收）+ `/api/permission` （native 主体经 `isAcpPermissionResponder` 钉死在自己那条活着的流上） | 适配器侧 `openpipal-acp/src/translator.ts` `case 'permission'` → `agent.ts` `createPermissionBridge` 反向调客户端 `session/request_permission`，裁决经 `http-client.ts` `respondPermission` 回传。**bridge 必须 fail closed**：不回传桌面端就永远 block 在等裁决 |
| 设置页「连接」tab 空着 / 状态不刷新 | 拼装在 `main/acp-status.ts`（会话来自 `config.acp` 标记，适配器不写标记就一条都不显示）；活跃态在 `main/acp-session-registry.ts` 纯内存，进程重启即清空 | 推送链路 `notifyAcpStatusChanged()` → `acp:status-changed` → `renderer/components/AcpConnections.tsx` 重新取快照；端口现问 `http-server.getHttpListeningPort()` |
| 编辑器里选不到自定义 Agent / 换人格没生效 | 适配器 `openpipal-acp/src/agent.ts` `personaOptions`（内置角色裸名 + 自定义 Agent `agent:<uuid>`，一个下拉两类）、`setPersona`、`syncPersonaFromDisk`（每轮开跑前对账并回推 current_mode_update / config_option_update）；切回内置角色必须同时发 `workspaceId: null`，不清就等于没切 | 桌面侧 `http-server.ts` PATCH `/api/conversations/:id` 收 `workspaceId` → `conversation-store.updateConversationWorkspace`，与 `updateConversationRole` **共用「开聊后拒绝」那把锁**；resume 以磁盘 `workspaceId` 为准 |
| 在桌面端改了会话，编辑器（Zed）那边没跟上 | 推送侧：`main/conversation-events.ts`（唯一发布出口，发布点在 `conversation-store` 四个写函数里）→ `http-server.ts` `GET /api/acp/events`（native 专用 SSE，带 ready + 15s 心跳）| 订阅侧：`openpipal-acp/src/agent.ts` `runDesktopEventLoop`（退避重连，只在状态翻转时打日志）→ `onConversationChanged` → `syncPersonaFromDisk`。**通道断了不影响正确性**：每轮开跑前还有一次对账兜底，最坏只是标签晚更正 |
| ACP 相关的已知问题（评审确认但还没修） | [acp-review-backlog.md](../acp-review-backlog.md)——15 条，带 file:line 与实测数字，按"值不值得现在修"排 | 已修的 15 条见该文件末尾；回归用例在 `conversation-config-atomicity.test.ts` / `protocol-compat-smoke.mjs` / `acp-desktop-live.spec.ts` / `teacher-skill-isolation.test.ts` |
| 装机版里 `npm` / `node` / `npx` command not found（dev 完全复现不了） | `main/login-shell-path.ts`（启动时问一次登录 shell 的 PATH，合并进 `process.env.PATH`），在 `main/index.ts` 的 `whenReady` 开头发车 | **macOS 双击图标启动的进程只有 `PATH=/usr/bin:/bin:/usr/sbin:/sbin`**（2026-08-25 实测，用最小 `.app` 经 Finder 启动测出来的）——node/npm/uv/pnpm/cargo/go 全不在里面，只有 git 和 python3 活着。终端起的进程继承完整 PATH，所以 `npm run dev` 和 **全部 Playwright E2E 都测不到这个洞**。想复现：`env -i HOME=$HOME SHELL=$SHELL PATH=/usr/bin:/bin:/usr/sbin:/sbin node_modules/electron/dist/Electron.app/Contents/MacOS/Electron .`，看日志里有没有 `[PATH] 补上登录 shell 的目录 N 个`。**注意别用 `open xxx.app` 做对照**：`open` 会把调用方环境带过去，测出来是假阴性，必须让 Finder 启动。同样受影响的还有 stdio MCP server（`mcp-manager.ts` 同样透传，命令常是 `npx`）与 **OS 级沙箱**（sandbox-runtime 要在 PATH 里找 `rg`，找不到就无声降级成应用层安全模型——日志里认这一行：`[Sandbox] 初始化失败…ripgrep (rg) not found`，好了之后是 `[Sandbox] 沙箱已启用（macOS Seatbelt）`）|
| 拿公开 benchmark 测编码助手（SWE-bench 等） | **配对对照** `tests/e2e/swebench-ab.spec.ts` + A 臂裸 CLI 驱动 `tests/e2e/bench-arms.ts` + 判分与题库准备 `tests/bench/`（流水线与全部纪律写在 `tests/bench/README.md`）；单题冒烟仍留 `tests/e2e/swebench-one.spec.ts` | **用途是对照不是刷分**：同一个模型/端点/凭据/快照/判分，「裸 `pi` CLI」对上「走我们这层壳」，**差值才是我们的价值**（Datacurve 的 DeepSWE 审计里这个差值是 69% → 81%，12 个点全在壳上）。**配对不是两组独立的题**：独立两组要看出 12 个点得上百道，配对只看不一致对（A过B没过 / B过A没过），几十道就出信号。**四条纪律**：① 防作弊——直接 clone 会把含标准答案的未来提交一起拉下来（DeepSWE 审计里 Opus 4.7 有 18% 的通过是 `git log --all` 读来的），快照只留 base_commit 那一刻、`.git` 重建成单条提交、test_patch 干完才打；② **标准答案闸门**——环境不是官方镜像，差一点就两条臂恒 ❌ 且不报错，所以只有「打上官方 patch 能判成解决」的题才准进题库（不花模型的钱；第一版没这道闸，15 道里 7 道根本判不了）；③ **两条臂就地跑同一个目录、跑完各自还原，不复制副本**——venv 的可编辑安装把模板绝对路径写死在 `__editable___<pkg>_finder.py` 里，副本里 `import <pkg>` 仍解析回模板，于是 agent 改副本永远不生效（两条臂都必然判负、不报错），聪明点的 agent 顺路径去改模板还会让 A 臂的修复漏给 B 臂（实测 3 个模板被写脏）；④ **判分子进程的环境必须显式构造，不能原样继承调用方**——Playwright 的 workerHost 给每个 worker 硬塞 `FORCE_COLOR=1`，pytest≥7 认它，`-rA` 摘要每行都以 ANSI 开头，而官方 parser 判 `line.startswith("PASSED")`，于是 `parsed=0`、**所有测试静默判负**、两条臂结果逐条相同、手跑还复现不了（普通 shell 没这变量）——40 道废了 16 道，A 臂真实分从"32.5%"变成 54.2%。**没有 `parsed` 和 agent 的实际 patch 就没法事后归因**，这两个字段是花了 40% 样本才补上的。**环境为什么不是官方 Docker**：本机直连 `registry-1.docker.io` 直接 EOF、daocloud 把 swebench 挡在白名单外，500 个镜像一个拉不到；改用 uv venv 就地建，只收 Python 小版本对得上且不用编 C/Cython 的仓库（django/sympy/pytest/pylint/requests/flask/xarray，253 道）。少数 P2P 因 Python 小版本天生红，按 baseline 逐题扣除、**两条臂扣同一批**，差值不受影响，绝对分因此不与官方榜单可比 |
| 编码助手的纪律有没有被真的遵守（不是「提示词里写没写」） | `tests/e2e/coding-discipline.spec.ts`（B1 脏工作区 / B2 交付物 / B3 退出码与报数 / B4 只读档汇报）、共用脚手架 `tests/e2e/headless-driver.ts` | **和 `coding-role-shape.test.ts` 分工不同**：那边是 `expect(systemPrompt).toContain('不许回退')`，保护的是**句子还在提示词里**；这边验**模型照没照做**。两者都要——今天有直接证据说明不是一回事：git 身份那次提示词写着「每条命令都要看退出码」、模型完全照做，照样把假署名写进历史（git 返回 0）。判据尽量落磁盘，非看回话不可时只用宽松正向匹配。**写这类用例自己最容易踩的两个坑**：① `node --test test/` 按**文件**记数（一个文件两条测试错一条 = `pass 0 / fail 1`），`node --test test/x.test.js` 才按**条目**记数——混用会让 `{pass:2}` 这种断言永远不可能成立；而且**这个写法本身就是坏的**（2026-08-25 实测 Node v22.21.1 / v25：目录参数被当模块解析，直接 `MODULE_NOT_FOUND`），夹具的 `npm test` 一度就写成它，于是每条真模型用例都先花几轮去修 harness 自己的 package.json，验的成了「它会不会修夹具」——夹具的 `npm test` 现在统一写不带参数的 `node --test`（自动发现），前提由 `tests/unit/coding-depth-fixtures.test.ts` 守着；② 助手回话**不能读 `/api/conversations/:id/messages`**，extension 来源的助手消息由客户端自己回写，harness 不回写就永远是空串——要从 SSE 的 `{type:'text',content}` 事件累。2026-08-24 首轮 4/4 全绿（一次 5.1 分钟，四条共用一个隔离实例）|
| 编码助手**会不会真干活**（跨文件重构 / 长任务 / 根因定位这些深度能力） | `tests/e2e/coding-depth.spec.ts`（C1 跨文件重构 · C2 长任务 · C3 含糊需求 · C4 根因定位 · C5 起草 AGENTS.md · C6 改动打红老测试）、夹具在 `tests/e2e/depth-fixtures.ts`，夹具前提由 `tests/unit/coding-depth-fixtures.test.ts` 守（不花钱，跟单测跑） | **判分手法是隐藏测试**：跑完之后再把测试文件写进仓库，agent 全程看不见它们——「只把眼前那条红测试弄绿」的贴膏药改法当场露馅，这是纯文本断言够不到的地方。C1 的可见测试只钉住 `config.json`，两个调用方漏改照样绿（隐藏测试专抓），另放一个诱饵：`src/vendor/` 里同名字段属第三方，一把梭 sed 就会连它一起改掉；C4 的症状在 `report.js`、病根在 `date.js` 的 `getMonth()` 没加一，在 report 里 +1 能骗过可见测试、骗不过点名 date/export 的隐藏测试。C5 是**两轮**的：`repo-onboarding` 技能明写「写盘前先问」，所以第一轮只该给草稿、第二轮点头才落盘；判据落在**可执行性**上——草稿和成品里每个 `npm run xxx` 都必须真存在于 package.json（编一个不存在的 `npm run lint` 是这类任务最常见的幻觉，而这份文档会被之后每个 AI 工具当成事实）。**写这类用例要知道的两件事**：① 夹具目录名固定，同一个 root 下建两次同名夹具会撞上已有 git 仓库、`git commit` 报 nothing to commit（所以 `build*Repo` 都收第二个参数换名）；② **auto 档下这个夹具一张授权卡都不弹**——工作目录在 `os.tmpdir()`（属 `ALLOWED_DIRS`）、沙箱已启用、命令又不在危险表里，六条用例实测 `permissions.length === 0`，所以「权限被拒之后不找绕法」在这里**验不了**，想验拒绝路径得先造出真会弹卡的场景。多轮驱动看 `headless-driver.ts` 的 `conversationId` + `history`：服务端不替 extension 来源回写助手消息，历史必须调用方自己带，只传 conversationId 模型会看到一句没有上文的「可以，写吧」。**2026-08-25 首轮 6/6 全绿**（一次 6.8 分钟，六条共用一个隔离实例，端口 3133）|
| 程序化驱动编码助手（benchmark harness / 自动化验收 / 接 ACP 前的地基） | `main/http-server.ts`（`/extension/session` 换 token → `/api/conversations` 建会话 → `/chat/stream` 发任务收 SSE → `/api/permission` 应答权限）、`main/local-http-auth.ts`（`BROWSER_EXACT_ROUTES` 决定哪些路由对插件主体开放） | **四步就能把编码助手当成可编程 agent**：`conversationConfig` 里带 `{role:'coding', workingDir, permissionTier}`，判据一律看磁盘不看回话。三个坑（2026-08-24 逐个实测踩过）：① **权限应答必须原样回传 SSE payload 里的 `executionId`** —— `resolveInlinePermission` 对非桌面来源要 `(conversationId, executionId)` 双对齐，少一个就是 `409 {ok:false}`，而 agent 会**静静等批准直到超时**（这道校验是对的：少了它本机任何调用方都能替别的会话点允许）；② 读 SSE **必须自己兜空闲/硬超时**，流末尾有 15s 一次心跳，光等 `done` 会在 agent 卡住时无声吊死（实测一次 10 分钟零输出）；③ 端口写死 3031 且被占时只打一行日志跳过 —— 用户 App 开着时第二个实例**根本没有 HTTP 面**，所以加了 `OPENPIPAL_HTTP_PORT` 覆盖（**只给隔离实例用**；插件与 ACP 适配器都写死认 3031，正式运行别动，不设变量时行为逐字节不变）。回归在 `tests/e2e/coding-agent-headless.spec.ts`（`OPENPIPAL_CODING_LIVE=1` 才跑：真模型，1 分半，含一次真实权限卡往返 + 推送落盘） |
| 编辑器要怎么启动适配器 / 打包后 ACP 连不上 | `main/acp-adapter-launch.ts`（打包态 `process.resourcesPath/acp/openpipal-acp.mjs`，开发态仓库里的 `openpipal-acp/dist/index.js`；文件不在返回 null）+ `electron-builder.yml` extraResources + `package.json` `build:acp` | 命令形如"用 App 自己当 Node"：`process.execPath` + `ELECTRON_RUN_AS_NODE=1`。**必须是 `.mjs`**——Resources 下没有 package.json，`.js` 会被当 CommonJS 解，import 直接语法报错。守卫在 `electron-builder-release-boundary.test.ts` |
| 编辑器的斜杠菜单少了角色专属技能 | `http-server.ts` `GET /api/skills` 的 `role` 参数 → `skill-manager.resolveSkillScope`（**列表与提示词索引共用它**，这是"模型看得见、菜单里没有"的唯一防线）；适配器侧 `agent.ts` `publishCommands` 传 `session.role` | 三档作用域：workspaceId → 只看自定义 Agent 自己的；role → 全局 + 该角色专属（角色专属优先且不受全局禁用影响）；都不传 → 只有全局。回归在 `teacher-skill-isolation.test.ts` |
| 编辑器里没有斜杠命令 / 点了没效果 / 产物不流式 / 会话列表翻不完 | 适配器 `openpipal-acp/src/agent.ts`：`publishCommands`（命令用通知发，必须晚于 session/new 的响应）、`applySkillCommand`（`/名` → `<skill-request>名</skill-request>`）、`listV2Sessions`（keyset 游标，不是 offset） | 桌面侧 `http-server.ts` `GET /api/skills`（**改完必须 `npx electron-vite build`，否则 out/main 还是旧的、新路由 404 成 HTML**）；流式产物只有 `artifact_delta`/`visualizer_delta` 有真增量，`tool_progress` 只有字符数 |
| `/goal` 设了不生效 / 不自动续跑 / 判定不回写 | 状态构造与读写只有一处：`main/conversation-goal.ts`（`buildConversationGoal` 的 maxTurns 是 goal loop 的行为契约，改了两边都变）；桌面走 `chat:set-goal` IPC，ACP 走 `/api/conversations/:id/goal` | 判定循环在 `pi-agent-service.ts` 「8.5 Goal checkpoint」：GoalChecker 出错**一律放过**（fallback=true→ok=true）、连续 3 轮判否强停、`ask_user` 永远打断；状态靠 `goal_update` 事件回写（ACP 由 http-server 落盘，渲染层不在场）|
| 窗口拖拽/定位 | `App.tsx`（title bar） | `main/window-tracker.ts` |
| 语音能力不如文字/工具用不了 | `main/realtime-tool-bridge.ts`（工具集+执行）、`main/voice-tool-truncate.ts`（结果保真，丢 id/path 找这里） | `main/realtime-session.ts`（`VOICE_ETIQUETTE` + `handleVoiceFunctionCall` 链式 + `create_response` 接管） |
| 语音转录/VAD/打断/记忆 | `main/realtime-provider.ts`（`turn_detection`/`input_audio_transcription`） | `renderer/hooks/useRealtimeVoice.ts`（采集/节拍/记忆触发）、`main/memory-extractor.ts`（每会话游标） |
| 浏览器控制（客户端经插件控浏览器/CDP） | `main/browser-control.ts`（:3031 反向 WS 通道）、`openpipal-extension/background.js`（`bcExecute` 走 chrome.debugger） | `main/browser-tools.ts`（`browser_*` pi-tools）、`main/browser-policy.ts` + `browser-policy-store.ts`（站点轴安全，接 `pi-security`） |
| 上下文超限/历史压缩/带图历史 400 | `main/context-window-policy.ts`（单个工具结果限长）+ `main/history-compactor.ts`（按模型 Token 窗口整体压缩，只改模型载荷不动 UI） | `main/config-manager.ts`（`supportsImages`/`contextWindow` 能力位）、`chatStore.ts` `toApiMessages`（图片与工具轨迹完整传递）；缓存断点见 `model-prompt-cache-guide.md` |
| 缓存命中率低/账单比预期贵（功能全正常） | `main/agent-runtime/pi-message-conversion.ts`（`buildRuntimeContextMessage` 实发与回放同源，字节差一个空白就断）；读侧三处历史投影必须带 `messageKind`：`chatStore.toApiMessages` / `scheduler.storedMessageToChatMessage` / `http-server.ts` 内联 map | 写侧"谁把 RC 快照存进磁盘"：桌面是 renderer（`chatStore` 的 `onRuntimeContext`），ACP/定时任务是 `pi-event-adapter.createTranscriptCollector.finishRuntimeContext()`。实测靠 `scripts/diagnostics/usage-report.mjs` 看每轮首次调用的命中率 |
| 模型配置/连接测试/baseUrl | `main/config-manager.ts`（testConnection 与对话同管线 + /v1 探测补全） | `renderer/components/ModelSettings.tsx` |
| 角色前置页/设计系统 chip/历史产物列表 | `renderer/components/RolePreflowPanel.tsx`（manifest 驱动）+ `system-agents/<role>/preflow.json` | `main/role-manager.ts` `listRoleAssets`、`main/artifact-store.ts` `listArtifactHistory`、简报注入 `pi-agent-service.ts` |
| 工作目录选不了/选完不生效（欢迎页与对话页表现不一致） | `renderer/components/shared/WorkingDirBar.tsx`（贴输入框边缘的目录条，两页共用一份；`placement` 只决定贴上还是贴下） | 挂载点：`WelcomePage.tsx`（输入框下）与 `InputBar.tsx`（输入框上）；状态在 `chatStore.conversationConfig.workingDir`，靠负外边距 + 输入框 `relative z-10` 压出"贴边"观感 |
| 选了工作目录但文件工具全被拒 / 想放宽可操作范围 | `main/pi-security.ts` `assessWorkspaceRoot`（判一个目录能不能当「允许根」：过宽 equal 表 + 配置树 subtree 表 + 家目录下单层点目录 + 别人的家目录，四类拒；返回 `code` 供渲染层取 i18n 文案）+ `isAllowedPath(filePath, workingDir)`（ALLOWED_DIRS ∪ 本次会话工作根） | 登记入口在 `authorizeToolCall` 的 `noteActiveWorkspaceRoot`——**不是** `createSecurityHook`（现役 pi-core 自己 new PiCoreToolAuthorizer，不经过那层，挂错就静默落空）；沙箱侧 `sandbox-manager.buildSandboxConfig` 的 allowWrite = ALLOWED_DIRS + 工具链缓存 + `listWorkspaceRoots()`，换全局目录走 `replaceGlobalWorkspaceRoot` 整表重置（bash 分支不做路径判定，OS 沙箱是它唯一写边界）。**所有路径比较都靠 `resolveRealPath` 走 `realpathSync.native` 规范化大小写**——macOS 默认 APFS 不区分大小写，用 JS 版 realpathSync 会让 `~/.SSH`、`/Users/BOWEN1` 绕过整层黑名单。三个选目录入口（设置页 / 目录条 / Agent 模板编辑器）共用 `config:validate-working-dir`。回归在 `pi-security-workspace-root.test.ts` |
| 助手不认项目规矩 / AGENTS.md 没生效 / 想改注入边界 | `main/agent-runtime/project-context.ts`（`loadProjectContext` 收集入口文档，`buildProjectContextPrompt` 拼 `<project_context>` 段，`projectContextSnapshot` 按 `会话+工作目录` 快照） | 文件优先级 `AGENTS.override.md > AGENTS.md > AGENTS.MD > CLAUDE.md > CLAUDE.MD`，与 pi-coding-agent 一致（回归用 pi 自己的实现钉住）；**三条边界**：只走 `[仓库根 .. 工作目录]`（没有 .git 就只看本层，绝不爬到 `~` 或 `/`）、工作目录落在 `~/.openpipal/**` 时整段不注入、软链解析后必须仍在仓库内（否则 `AGENTS.md -> ~/.ssh/id_rsa` 能绕开 pi-security 的读取黑名单，这里是直接 readFileSync）。整段 8k token 封顶，就近优先吃预算，单份超了留头尾并标 `truncated`。注入点在 `openpipal-prompt-core.ts` 的 `render()`（工作空间之后、`<conversation-brief>` 之前）；UI 信号是目录条上的"已读 AGENTS.md"徽标，走 `config:describe-project-context`。回归在 `project-context-injection.test.ts` / `project-context-prompt-wiring.test.ts` |
| 改编码助手（提示词 / 工具集 / 前置页） | `main/roles/coding-role.ts`（角色定义 + 提示词）、`resources/system-agents/coding/preflow.json`（前置页） | **工具给全 = COMMON_TOOLS**（design 是 `[...COMMON_TOOLS, 'questions_v2']`，只多一个重面板的 intake 工具）。曾把产物族/浏览器族摘掉过，2026-08-22 撤回：那属于"预测模型会拿 HTML 报告顶替 diff"的**能力拐杖**，而交付物走错方向是可逆错误、够不上硬拒绝那一档；代价却很实在——内置技能里 13 个（dc-authoring / frontend-design / web-research 等）点名要的正是被摘的那些工具，摘完技能索引里躺着一排跑不动的条目。纪律改写进提示词（"别拿产物顶替改动" + "说不清就画"）。因此落盘 agent.md 写 `*`，加载时展开成**当时**的 COMMON_TOOLS，公共表增删自动跟随。提示词刻意薄（2k 字符，测试卡 6KB）——编码的专业知识住在用户仓库里（见上一行的入口文档注入），不往角色里搬；纪律条目对着 pi `dist/core/system-prompt.js`、Codex `gpt-5.2-codex_prompt.md`、DeepSeek Harness code-mode 快照、Claude Code 输出契约逐条比对补齐（脏工作区 / 退出码 / 注释分寸 / 最终消息形状），文件头记了每条的来路与刻意没抄的部分。前置页只做一件事：选仓库（`workingDir: { enabled: true }` 把 `WorkingDirBar` 挂进 preflow——preflow 会整页替换欢迎页，不挂就没地方选目录）；`fields: []` 意味着没有模板卡，连带也没有"空白会话"链接（那条链接是给必选卡准备的逃生口）。回归在 `coding-role-shape.test.ts` / `tests/e2e/coding-preflow.spec.ts`。角色专属技能住 `resources/system-agents/coding/skills/<name>/SKILL.md`（当前只有 `repo-onboarding`：陌生仓库定位流程 + AGENTS.md 起草纪律），走 `getBuiltInRoleSkillsDir`，**只对 coding 可见、不进全局 SkillsHub、不受全局禁用列表影响**；全局内置技能照常并进它的索引（约 3.3k token / 20 条，工具给全后都能真跑）。回归在 `coding-repo-onboarding-skill.test.ts`。**整体验收（2026-08-24，真模型 deepseek-v4-flash-vision-exp）**：`tests/e2e/coding-agent-live.spec.ts`（`OPENPIPAL_CODING_LIVE=1` 才跑，`OPENPIPAL_LIVE_MODEL` 只换模型名、端点与 key 照抄用户配置）。一句话任务「修红测试 → 跑通 → 提交 → 推到 origin」一次过（1m36s）：测试 3过1红 → 4过0红、裸仓真收到提交、提交标题守住 AGENTS.md 三条硬规矩（中文 / 不带 `feat:` 前缀 / 没碰 `test/`）——**提交标题是「入口文档真进了上下文」的唯一硬证据**，比看日志读没读过文件可靠。**Playwright 驱动这三处会踩空**：① `config.role` 不算数，`WelcomePage` 写死 `useState('general')`，得真点 `[title="编码助手"]`；② 工作目录与权限档都是**每条会话自己的**（`conversationConfig`），界面上选目录会开系统文件对话框、档位藏在菜单里，都驱不动，直接调 `__chatStore` 的 `setConversationWorkingDir` / `setConversationPermissionTier`；③ coding 有 preflow、整页替换欢迎页，输入框与发送键是 `preflow-input` / `preflow-start-btn`，等 `send-btn` 会一直等一个根本没渲染的按钮。权限卡按钮**没有 testid**，只能按文案 `允许` / `本次会话允许此类操作` 定位。App 中途「消失」先抓主进程 stdout/stderr（`app.process()`）：Playwright 只会说 page closed，死因全在那儿（实案：模型端点 network_error 连流都开不起来，与产品无关，白查了两轮）。 |
| 编码助手的权限档位（只读 / 自动审核 / 完全允许） | `main/pi-security.ts`（`PermissionTier`、`READONLY_TIER_TOOLS`、`authorizeToolCall` 里两处短路）、`renderer/src/components/shared/PermissionTierControl.tsx`（输入框胶囊） | 会话级、**只给 coding**：门在 `agent-overrides.ts`（`roleName === 'coding'`）——ACP/HTTP 那条路上外部客户端能 PATCH 会话 config，放宽必须有门、收紧不必。**`full` 只吃掉 `needs_confirmation` 的弹框**，不放行 `risky`、不越过 `assessToolScope`，也吃不掉带 `alwaysConfirm` 的那一类（破坏性命令 `rm -rf`/`git reset --hard`/强推、主目录全盘遍历、远程 MCP）——第一版漏了破坏性那条，测试才逮住：它们是 confirm 档不是 blocked 档，一短路就等于替用户裁决抹改动。`readonly` 做**两层**：`filterOpenPipalTools` 在 schema 层收窄（模型看不到写工具，才不会反复找绕法）+ 授权层拒绝兜底，外加 `openpipal-prompt-core.ts` 的提示段（不说清楚它会把"没有编辑工具"当环境故障）。白名单显式 15 个，**刻意不含 `bash`**（能 `echo x > file`，"命令是否只读"不可靠判定）与 `subagent`（档位不往下传）。回归在 `permission-tier.test.ts` / `tests/e2e/permission-tier.spec.ts`。**三档真机验收（2026-08-24，真模型，同一句 `git push origin main`）**：只读档 → 一个字都没动（裸仓 sha 不变、工作区 clean、零弹窗），它自己回话点明「本次会话是只读档，我手上只有读取类工具、没有 bash」并给出切档建议——说明 schema 层收窄那一层**真的到了模型面前**，不是靠授权层兜底；自动审核档 → 弹 1 次 git 凭据窗，点「本次会话允许」后落盘；完全允许档 → **零弹窗**直接推成功。三条都在 `tests/e2e/coding-agent-live.spec.ts`。 |
| 编码助手要 push / fetch / 用 `gh`（git 凭据的项目轴授权） | `main/git-policy.ts`（纯判据：`detectGitRemoteUse` 认命令、`decideGitAccess` 按档位裁决）、`main/git-policy-store.ts`（状态：持久 allowlist `~/.openpipal/git-policy.json` + 本对话内存授权） | **这一条是收紧不是放宽**：2026-08-23 实测沙箱里钥匙串 helper 通（wrap 前后行为一致、都 exit 0）、`~/.gitconfig` 也读得到，而 `git push origin main` 不在 DESTRUCTIVE_COMMANDS 表里（表里只有强推）、沙箱下判 `safe`——门加上之前模型能拿用户已存的凭据直接推代码，一次不问。三档语义按用户定的：只读拒、自动审核问一次并记住这个项目、完全允许放行。**授权单位是仓库不是 cwd**（`resolveProjectKey` 往上找最近的 `.git`）：按 cwd 精确匹配会让 `packages/web` 每个子目录重问一遍，按前缀匹配又会让「授权 ~/code」顺带授权底下所有仓库。路径一律 `realpathSync.native` 规范化（同 pi-security 的理由）。门挂在 `authorizeToolCall` 里 `classifyToolRisk` **之前**——分类那层没有 conversationId、也不知道谁授权过。「允许」记本对话、「本次会话允许」才落盘持久授权（走 ipc-handlers 的 `pendingGitGrant`，镜像 `pendingBrowserGrant`）；授权用的目录必须取请求自带的 `workingDir`，`getWorkingDir()` 是全局值、会把授权记到别的仓库上。**`git` 自己不读 `GITHUB_TOKEN`**（实测：git 二进制里 `GIT_TERMINAL_PROMPT` 命中 2 次、`GITHUB_TOKEN` 命中 0 次）——裸 `git push` 走凭据 helper（macOS 钥匙串），token 通道受益的是 `gh`/`glab`/`hub`（它们此前被无条件抹 token，等于完全不可用）。**凭据桥 `main/git-credential-bridge.ts`**：macOS 上 github.com 的 helper 常是 `!gh auth git-credential`，而 `~/.config/gh` 在拒读表里 → 沙箱内任何鉴权 git 操作都死在 `open ~/.config/gh/config.yml: operation not permitted`；且 **`gh` 在读配置文件之前不看 `GH_TOKEN`**，光给环境变量救不回来（都是实测）。补法：主进程在沙箱外 `gh auth token` 取到 token，**不放开那个目录**，只给这条命令挂内联 credential helper （`GIT_CONFIG_KEY_n=credential.https://<host>.helper`，**先写一条空值把已有 helper 列表清掉**——少了这条 gh 那个还会被调到并死掉，反证过会红）。两个坑：① helper 值以 `!` 开头，**绝不能经过 shell 字符串**（会被转义成 `\!`，git 转而去找名为 `credential-\!f() {…}` 的外部程序，实测踩过），只能走 spawn 的 env；② `OPENPIPAL_GIT_TOKEN` 结尾是 `_TOKEN`，不登记进 `GIT_CREDENTIAL_ENV_KEYS` 就会被清洗正则删掉、整套是死的。`gh` 本身在沙箱里仍不可用：给 `GH_CONFIG_DIR` 能越过配置那关，但 TLS 校验挂在 Go 的 macOS 证书链（`x509: OSStatus -26276`，`SSL_CERT_FILE` 无效）——SRT+Go 的问题，没做兜底。token 通道在 `sanitizeEnvironment(source, { allowGitCredentials })`——**陷阱**：`GITHUB_TOKEN` 同时被 `SENSITIVE_ENV_KEYS` 和 `/_(KEY\|SECRET\|TOKEN\|...)$/i` 删，所以是「先留底、删干净、再盖回」，只在一处开口子这开关就是死的。执行层只在「命令真要连远端」且「项目已授权」两条都成立时下发 token（`openpipal-execution-env.ts`），这样授权过的项目里 `npm install` 的 postinstall 脚本仍看不到 token。SSH 通道天生走不通（SRT 只代理 HTTP/HTTPS、沙箱内不放行 DNS）。授权文件已进 `getCredentialReadDenyPaths()`——能改它就等于能给自己授权。**沙箱下 `git init`/`git clone` 另有一坑（与凭据无关）**：denyWrite 里 `'/**/.git/hooks/*'` 必须留着（写钩子 = 下次 commit 执行任意代码），而 git 每次建仓都要拷默认模板的 `hooks/*.sample`，于是这两条命令在沙箱里 **100% 失败**（`fatal: cannot copy '.../templates/hooks/commit-msg.sample' … Operation not permitted`）。修法是 `getGitTemplateDir()`（sandbox-manager）指一个空目录，沙箱开着时注入 `GIT_TEMPLATE_DIR`，**拒写规则一个字不动**；注入要排在 `safeEnv` 之前，否则会盖掉用户自己设的那份。实测：默认模板拷 14 个 sample，空模板拷 0 个；改完 `git init`/`clone` 都 exit 0，而往 `.git/hooks/pre-commit` 写仍然被拒。**端到端验收（2026-08-23，真沙箱 + 真 GitHub 私仓 + 真 token）**：未授权 push → `open ~/.config/gh/config.yml: operation not permitted` + `could not read Username`；授权后 push exit 0 且本地/远端 sha 一致；clone 私有仓授权前后 128 → 0；已授权仓里不碰远端的命令 `OPENPIPAL_GIT_TOKEN` 仍为 unset。回归在 `git-policy.test.ts` |
| 输入框打 `/` 不出面板 / 技能没被强调 / 路径被当成指令 | `renderer/components/shared/SkillMention.tsx`（`/` 面板 + 镜像层着色；`commands` 只在行首给，InputBar 传 `/goal`，欢迎页不传） | 词法在 `renderer/chat/skillRequest.ts`：`isTriggerBoundary`（行首或空白后的 `/` 才算触发符，`src/pdf` 因此不命中）、`findSkillMentions` 只认已知名单、`expandSkillMentions` 发送时换成 `<skill-request>`；编辑器侧同一套说法在 `openpipal-acp/src/agent.ts` `applySkillCommand` |
| 输入框上传（+ 选文件/拖拽/粘贴路径）分流不对 | `renderer/components/shared/useComposerFileIntake.ts`（图片内联 / study 布局收进知识库 / 其余上传成待发附件，两页共用一份规则） | 挂载点：`WelcomePage.tsx` 与 `InputBar.tsx`（两页 + 按钮都直开选择器，对话页另有拖拽与粘贴路径）；附件状态在 `chatStore.pendingFileAttachments`，发送时只传路径不进正文 |
| 自检实时画面（输入框上方固定槽/设备外框） | `renderer/components/SelfCheckPreview.tsx`（卡 + 模块级 store）+ `renderer/chat/selfCheck.ts`（纯逻辑：外框选择/结论解析/目标产物回溯）；挂载点在 `App.tsx` ChatPanel 与 InputBar 之间 | 触发靠 `liveStreamStore.toolStatus === 'render_artifact'`；自检本体在 `main/pi-tools.ts` render_artifact（隐藏窗口真渲染+console+重叠 lint+文本摘要）。结论文本走 tool-end 的 `mcpResult` 位（`pi-event-adapter` 通用分支把**所有**内置工具的结果文本都放这个位置，名字带 mcp 只是历史包袱） |
| 设计系统查看（画廊卡墙/文件浏览器/顶栏切换） | `renderer/components/artifacts/DesignSystemView.tsx`（外壳+「全部文件」下拉）→ `DesignSystemGallery.tsx`（卡墙+逐卡评审）/ `DesignSystemFiles.tsx`（Finder 式，≥560px 分栏、窄版推进式） | `main/role-manager.ts` `getDesignSystemManifest`（`groups`/`kits` 给画廊、`files` 给文件视图，`scanDsFiles` 如实扫盘）；文本/图片走受控 API，iframe 走按单套系统绑定的进程期只读 capability 路径 |
| 插件（Agent Plugins 包）装不上/技能或 MCP 没生效 | `main/plugin-manager.ts`（manifest 校验+组件发现，失败原因都在 `warnings`/`invalid` 字段） | `main/plugin-import.ts`（安装/卸载/启停）、技能接入点 `skill-manager.ts` `scanAllSkills` skillPaths、MCP 接入点 `mcp-manager.ts` `initMcpServers` 尾批（server 名带 `<plugin>:` 前缀）；UI `ToolsHub.tsx` PluginsTab |
| 画布圈画/点选评论、Tweaks 停靠条、Reload 门闩（预览不更新/更新打断操作） | `artifacts/HtmlPreview.tsx`（BRIDGE_SCRIPT `_onStroke*` 圈画 + `ui:interact` 上报；宿主 `isCanvasBusy`/`applyPendingDoc` 门闩、`submitStrokeComment` 截图链） | `DcTweaksPanel.tsx`（停靠条）、`chatStore.pendingAnnotations` + `InputBar.tsx`（`<canvas-annotation>` 拼接+截图并入 images）、IPC `window:capture-region`（对可见窗口 `capturePage`，非系统截屏权限路径） |
| design 查出别的规划/项目的内容（串味） | `pi-agent-service.ts` 资产库注入段（`workspace/assets/<role>/` 是**跨会话常驻**库，system prompt 指示模型主动 ls） | `ipc-handlers.ts` `assets:upload-to-category`（preflow 来源落 `workspace/uploads/`，**不再入库**）；简报按会话注入走 `initialAssets` |

## 新增 pi-tool 必须三处登记

加工具只在 `pi-tools.ts` 写定义**不够**。任何漏掉都会让 AI 看不到、或走保守路径（默认 confirm）导致 IPC 卡死。历史教训：`present_to_user` 漏了 pi-security 登记，序列化大 HTML 参数时 IPC 失败 → 30 秒权限超时 → 自动拒绝 → 用户看到"长时间无反应"。

1. **`src/main/pi-tools.ts`** — 工具定义（name / description / parameters / execute）
2. **`src/main/role-manager.ts` COMMON_TOOLS** — 白名单（否则 `isToolAllowed` 过滤掉，AI 根本收不到 schema）
3. **`src/main/pi-security.ts` `classifyToolRisk`** — 安全分类（默认走 `needs_confirmation`，大参数工具会把 IPC 卡死）

排查顺序：AI 没调工具 → 查 role-manager 白名单；AI 看到但执行卡住 → 查 pi-security 分类。

**改（尤其是删/改名）COMMON_TOOLS 时还要看提示词**：角色提示词里写着"用 `grep` 找""改用 `edit`"，工具没了而提示词还留着，模型会绕道用 bash 或按提示词的说法编个结果，编译和别的测试都不会红。`tests/unit/role-prompt-tool-drift.test.ts` 两条规则各盯一种漂移——反引号里的标识符必须还是真工具（抓删除/改名）、点名的工具必须在该角色表里（抓角色收窄，权限档位与浏览器工具条件注入都属这类）。两种失效都做过反证：删掉 `grep` / 把 `edit` 从编码助手表里拿掉，各自会红。

## 新增 dc 预制件（starter component）必须五处登记

与 pi-tool 三处登记同类风险：任何漏项在对应渲染/导出路径**静默失效**（预览白屏 placeholder / 导出裂引用），无报错。以 ios-frame（2026-07 移植）为对照样例：

1. **`resources/dc-runtime/<name>.js(x)`** — 预制件本体（官方原文件照搬，头部保留 `@ds-adherence-ignore` 注释）。含 JSX 的还要一份 `<name>.compiled.js`（esbuild 转译 + animations.compiled.js 同款 React 就绪轮询包装，离线免 Babel）
2. **`src/renderer/src/components/artifacts/dcRuntime.ts`** — `?raw` 导入 + `KNOWN_SIBLINGS` fromRe + `resolveKnownRaw`（预览内联，共两三处同文件）
3. **`src/main/dc-headless.ts` resolve** — render_artifact 自检 / PDF/MP4 无头渲染
4. **`src/main/dc-export.ts` resolveExportSibling** — 导出随包拷贝（JSX 预制件 copyFrom 用 compiled 版）
5. **技能文档** — `dc-authoring/SKILL.md` 预制件清单（"唯 N 支持"计数要改！）+ 对应形态技能的用法节；顺手补 `dc-handoff-export.ts describeDesignFile` 的友好名

排查顺序：预览 placeholder → 查 dcRuntime.ts；导出打开裂 → 查 dc-export.ts；render_artifact 自检空白 → 查 dc-headless.ts；模型不用新预制件 → 查技能文档"唯 N"清单。

**`grep`/`find` 默认看不见 `node_modules`（2026-08-26 补了 `includeIgnored` 口子）**：两个工具遵守 `.gitignore`（`openpipal-search-tools.ts` 的 `GREP_IGNORE_FILE_NAMES` / `FIND_IGNORE_FILE_NAMES`），而本仓 `.gitignore` 第一行就是 `node_modules`——于是**助手搜不到自己脚下的 pi 运行时**（dist 里 401 个 .js/.d.ts），尽管我们源码有 5 处直接 import 它的具体路径（`pi-tools.ts:12`、`evolver-tools.ts:7`）。影子运行实测过一次代价：任务明写「说清楚你凭什么定这个超时值」，权威答案 `DEFAULT_HTTP_IDLE_TIMEOUT_MS = 300_000` 就躺在依赖包第 3 行，它搜不到、只好用推理补上——把判死线定成了用户口中的「正常等待上限」，零余量。现在两个工具都有 `includeIgnored`（**按次 opt-in，不传时代码路径走不到、行为逐字节不变**）；实现是「传了就把忽略文件名列表换成空数组」，空规则集的 matcher 对谁都返回 false，walk 里不必开分支。**`.git` 与 `.env*` 的排除不在这一层**——它们在 `walkDescendants` 里硬编码跳过，所以放开 `.gitignore` 动不到它们（SWE-bench 防作弊靠的就是 `.git` 挡得住，这条不能破）。回归在 `openpipal-search-tools.test.ts` 的 `includeIgnored reaches dependencies...`，一条用例同时钉住「默认搜不到」「开了能搜到」「开了也照样搜不到 .git/.env」三件事。

反向陷阱（2026-07 实案，一坑两跌）：白名单登记了但**工具实体从未创建**——`createCodingTools()` 只含 read/bash/edit/write，grep/find/ls 必须再调 `createGrepTool/createFindTool/createLsTool` 显式创建。白名单对不存在的名字静默落空、无任何报错，症状是"模型顽固用 bash 摸目录"，先后两轮被误诊为提示词/模型习惯问题。补上实体后又跌进第三处：`classifyToolRisk` 的 READONLY/文件分支都不认识它们 → 每次列目录弹确认、一次内联请求挂满 1800s 超时自动拒绝（注意 pi-security 有**两套**登记：`assessToolScope` 认识 ≠ `classifyToolRisk` 认识）。行为异常先对账"环境给没给能力"，再怀疑模型没听话。

## 安全体系

三层安全：分类器（safe/risky/confirm）→ 用户确认弹窗 → 路径黑名单（.ssh/.aws/.gnupg）。源文件 `src/main/pi-security.ts`，详见 [../SECURITY.md](../SECURITY.md)。新增工具/MCP 服务时必须检查安全分类和路径边界。
