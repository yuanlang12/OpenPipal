/**
 * Pal（用户自建的独立智能体）的跨进程约定。
 *
 * Pal 的人设来自它自己的 agent.md，技能 / 记忆 / 规则 / 工具配置都在它自己的目录里；
 * 对话记录里的 `role` 字段是 Pal 出现之前"一切皆角色"的老结构，对 Pal 只是个技术槽位。
 * 这个槽位必须是**中性值**：general 没有专属提示词、没有产物闸门、没有角色专属技能，只带公共工具。
 * 以前这里塞的是 App 当时选中的全局角色——用户上次点过设计助手，Pal 就借来 design 的
 * "整页 HTML 必须是 DC"闸门，而 Pal 又看不到 design 的技能，两头一凑就卡死（2026-09-09 实撞）。
 *
 * 下一步（统一身份）：对话只留一个 agentId（内置名或 Pal id），角色专属行为改成各自目录里的声明；
 * 到那时这个常量随 `role` 字段一起退场。
 */
export const PAL_BASE_ROLE = 'general'
