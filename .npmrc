# eslint-plugin-react 的 peer 范围停在 eslint ^9.7（7.37.5 是当前最新版，上游还没跟上
# eslint 10），而本项目用 eslint ^10.2.0。实测 eslint 10.2.0 + 该插件工作正常——
# 卡住的只是元数据，不是运行时。
#
# 没有这一行，`npm ci` 和 `npm install` 都会直接 ERESOLVE 失败，新克隆仓库的人第一步
# 就撞墙。写在这里而不是让每个人手敲 --legacy-peer-deps，是为了让 `npm ci`（锁定、可
# 复现的安装方式）继续可用——否则大家只能退回 `npm install`，锁文件迟早漂移。
#
# 上游放宽 peer 范围后即可删除本文件。
legacy-peer-deps=true
